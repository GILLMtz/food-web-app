import { BadgeChangeDirective } from './badge-change.directive';

describe('BadgeChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new BadgeChangeDirective();
    expect(directive).toBeTruthy();
  });
});
